{
  "scripts": {
    "deploy": "cd Frontend && npm run build && cd .. && git add . && git commit -am \"Deployment\" && git push -u origin main"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "nodemailer": "^6.9.12",
    "nodemon": "^3.1.0"
  }
}
